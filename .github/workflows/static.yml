{name: Deploy React App to GitHub Pages, 'on': {push: {branches: [main]}, workflow_dispatch: null}, permissions: {contents: read, pages: write, id-token: write}, concurrency: {group: 'pages-${{ github.ref }}', cancel-in-progress: true}, jobs: {deploy: {runs-on: ubuntu-latest, environment: {name: github-pages, url: '${{ steps.deploy.outputs.page_url }}'}, steps: [{name: Deploy React App, id: deploy, uses: bitovi/github-actions-react-to-github-pages@v1.2.4, with: {path: build, install_command: npm ci --prefer-offline --no-audit --no-fund, build_command: npm run build, caching: true}}]}}}
